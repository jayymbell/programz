<%= form_with(model: program_role, local: true) do |form| %>
  <% if program_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(program_role.errors.count, "error") %> prohibited this program_role from being saved:</h2>

      <ul>
      <% program_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :program_id %>
    <%= form.text_field :program_id, id: :program_role_program_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :program_role_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :program_role_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
